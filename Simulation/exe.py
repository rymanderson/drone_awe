import awesomeModelName_importer

Dronename = 'dji-Mavic2'
SoH = 90

with open('textfile.txt','r') as filename

# from google doc:
Drone = readline
If drone==true:
	Dronename = readline
	…
	drone = Drone(Dronename…)
Else
	error(‘Must specify drone name’)

# instantiate battery
battery = Battery(drone)

#get class initialization info
Open settings.txt
	Readline…
	If rain==true:
		Dropsize = readline
		WVC = readline
		…
		rain = Rain(dropsize,WVC,...)
	Else
		For i=1:rainlength:
			Readline

	#simulation variables
	Timestep = readline
	…
	#plot settings
	##time plots
	Totalpower	true
	Rainintensity false
	…
    # VVV In text file itself VVV
    # ##time invariant plots: select two parameters from list:
	# ### range payload endurance …
	# #### example:
	# #### range	payload
	# plot range payload
	# ^^^ 		         ^^^
	while eof == false:
	next = readline()
	if next==plot:
		plotx.push(readline())
		ploty.push(readline())
		Else:
			error(“invalid plot syntax”)

simulation = Simulation(drone,weather,power,...timestep,...)
simulation.run

plotter = Plotter(inputs….)
plotter.plot(
